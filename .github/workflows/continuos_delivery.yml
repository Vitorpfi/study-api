name: Building

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:

  build:

    runs-on: ubuntu-latest
    env: 
      DOCKER_USER: vitorpfi
      DOCKER_PASSWORD: dckr_pat_TbshBzswebv-BIadYWNL1Oq7MbE
      REPOSITORY: study-api
       
    steps:
    - uses: actions/checkout@v3
    - name: docker login

      run: |
        docker login -u vitorpfi -p dckr_pat_TbshBzswebv-BIadYWNL1Oq7MbE
    - name: build image
      run: docker build -t vitorpfi/study-api .
    - name: push to registry
      run: docker push vitorpfi/study-api